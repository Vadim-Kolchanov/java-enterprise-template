# lib-gateway-starter

The starter library provides a set of reusable components and configurations for building API gateways using Spring
Cloud Gateway. The library includes pre-configured filters, handlers, and configs that can be easily extended and
customized according to the project's needs.

## Depends on internal modules

* [lib-exception-handler](../lib-exception-handler)